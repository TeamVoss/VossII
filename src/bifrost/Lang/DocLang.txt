The Language Lang
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Lang==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Lang are the following:
  | ``action`` | ``actiontype`` | ``alias`` | ``auto``
  | ``by`` | ``call`` | ``define`` | ``do``
  | ``else`` | ``external`` | ``fields`` | ``fl``
  | ``flavor`` | ``for`` | ``foreach`` | ``function``
  | ``goto`` | ``if`` | ``in`` | ``inline``
  | ``label`` | ``module`` | ``numeric`` | ``off``
  | ``on`` | ``protocol`` | ``provided`` | ``reading``
  | ``return`` | ``setpower`` | ``state`` | ``then``
  | ``type`` | ``unsigned`` | ``var`` | ``via``
  | ``while`` | ``writing`` |  |

The symbols used in Lang are the following:
  | ; | = | : | [
  | ] | , | ( | )
  | => | -> | () | {
  | } | += | -= | ++
  | -- | --%<-- | ... | {{
  | }} | | | & | ^
  | == | != | < | >
  | <= | >= | + | -
  | * | / | % | <<
  | >> | |>> | ~ | ::
  | --> | . | _ |

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of Lang==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[TopLevel]//
  | //[TopLevel]// | -> | **eps**
  |  |  **|**  | //TopLevel// //[TopLevel]//
  | //TopLevel// | -> | //Decl// ``;``
  |  |  **|**  | //SubDef//
  | //Decl// | -> | ``module`` //Ident//
  |  |  **|**  | ``protocol`` //Protocol//
  |  |  **|**  | ``flavor`` //String//
  |  |  **|**  | ``state`` //[StateAspect]//
  |  |  **|**  | ``actiontype`` //ActionTypeName// ``=`` //ActionType//
  |  |  **|**  | ``action`` //ActionName// ``:`` //ActionType1// //ActionArrayInfo// ``provided`` ``by`` //Provider// ``via`` //Protocol//
  |  |  **|**  | //VarDecl//
  |  |  **|**  | ``type`` //TypeName// //TypeCompileDefinition//
  |  |  **|**  | ``alias`` //TypeName// ``=`` //Type//
  |  |  **|**  | ``fields`` //TypeName// ``[`` //[FieldDef]// ``]``
  |  |  **|**  | ``numeric`` //TypeName// ``unsigned`` //Integer//
  |  |  **|**  | ``define`` //DefineName// //[DefineParam]// ``=`` //Exp//
  | //[FieldDef]// | -> | **eps**
  |  |  **|**  | //FieldDef//
  |  |  **|**  | //FieldDef// ``,`` //[FieldDef]//
  | //[DefineParam]// | -> | **eps**
  |  |  **|**  | //DefineParam// //[DefineParam]//
  | //VarDecl// | -> | ``var`` //Type1// //[VarName]//
  | //[VarName]// | -> | //VarName//
  |  |  **|**  | //VarName// ``,`` //[VarName]//
  | //StateAspects// | -> | ``[`` //[StateAspect]// ``]``
  | //[StateAspect]// | -> | **eps**
  |  |  **|**  | //StateAspect//
  |  |  **|**  | //StateAspect// ``,`` //[StateAspect]//
  | //ActionArrayInfo// | -> | **eps**
  |  |  **|**  | ``[`` //Exp// ``]``
  | //Provider// | -> | ``external``
  |  |  **|**  | //String//
  |  |  **|**  | ``(`` //String// //[ActionName]// ``)``
  | //[ActionName]// | -> | **eps**
  |  |  **|**  | //ActionName// //[ActionName]//
  | //Protocol// | -> | ``auto``
  |  |  **|**  | //Exp//
  | //TypeCompileDefinition// | -> | ``=>`` //String//
  |  |  **|**  | **eps**
  | //FieldDef// | -> | //FieldName// ``:`` //Type//
  | //FieldName// | -> | //Ident//
  |  |  **|**  | //String//
  | //FunPat// | -> | //Pattern//
  |  |  **|**  | //Pattern// ``->`` //FunPat//
  | //Pattern// | -> | //Param//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Pattern// ``,`` //[Pattern]// ``)``
  | //[Pattern]// | -> | **eps**
  |  |  **|**  | //Pattern//
  |  |  **|**  | //Pattern// ``,`` //[Pattern]//
  | //Param// | -> | //Ident// ``:`` //Type1//
  | //ActionType// | -> | //FunPat// ``reading`` //StateAspects// ``writing`` //StateAspects//
  |  |  **|**  | //ActionType1//
  | //ActionType1// | -> | //ActionTypeName//
  |  |  **|**  | //ActionType2//
  | //ActionType2// | -> | ``(`` //ActionType// ``)``
  | //Type// | -> | //Type1// ``->`` //Type//
  |  |  **|**  | //Type1//
  | //Type1// | -> | //Ident//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Type// ``,`` //[Type]// ``)``
  |  |  **|**  | ``(`` //Type// ``)``
  | //[Type]// | -> | //Type//
  |  |  **|**  | //Type// ``,`` //[Type]//
  | //SubDef// | -> | //[SubFlag]// ``function`` //SubroutineName// ``:`` //FunPat// ``{`` //[SubLine]// ``}``
  | //[SubFlag]// | -> | **eps**
  |  |  **|**  | //SubFlag// //[SubFlag]//
  | //[SubLine]// | -> | **eps**
  |  |  **|**  | //SubLine// //[SubLine]//
  | //SubFlag// | -> | ``inline``
  | //SubLine// | -> | //VarDecl// ``;``
  |  |  **|**  | //Stm//
  | //Stm// | -> | //NormalStm// ``;``
  |  |  **|**  | //AbnormalStm//
  | //NormalStm// | -> | //Assignish//
  |  |  **|**  | ``return``
  |  |  **|**  | ``goto`` //GotoLabel//
  |  |  **|**  | ``setpower`` //Power// //ActionName//
  | //Power// | -> | ``on``
  |  |  **|**  | ``off``
  | //Assignish// | -> | //Exp// ``=`` //Exp//
  |  |  **|**  | //Exp//
  |  |  **|**  | //Exp// ``+=`` //Exp//
  |  |  **|**  | //Exp// ``-=`` //Exp//
  |  |  **|**  | //Exp// ``++``
  |  |  **|**  | //Exp// ``--``
  | //AbnormalStm// | -> | ``label`` //GotoLabel// ``:``
  |  |  **|**  | ``--%<--``
  |  |  **|**  | ``while`` ``(`` //Exp// ``)`` //StmBlock//
  |  |  **|**  | ``for`` ``(`` //Assignish// ``;`` //Exp// ``;`` //Assignish// ``)`` //StmBlock//
  |  |  **|**  | ``foreach`` ``(`` //VarName// ``in`` //Exp// ``...`` //Exp// ``)`` //StmBlock//
  |  |  **|**  | //StmBlock//
  |  |  **|**  | //IfLikeStm//
  | //IfLikeStm// | -> | ``if`` ``(`` //Exp// ``)`` //StmBlock// ``else`` //PostElseStm//
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //StmBlock//
  | //PostElseStm// | -> | //IfLikeStm//
  |  |  **|**  | //StmBlock//
  | //StmBlock// | -> | ``{`` //[Stm]// ``}``
  | //[Stm]// | -> | **eps**
  |  |  **|**  | //Stm// //[Stm]//
  | //StateAspect// | -> | //Ident//
  | //TypeName// | -> | //Ident//
  | //VarName// | -> | //Ident//
  | //ActionTypeName// | -> | //Ident//
  | //ActionName// | -> | //Ident//
  | //DefineName// | -> | //Ident//
  | //DefineParam// | -> | //Ident//
  | //SubroutineName// | -> | //Ident//
  | //GotoLabel// | -> | //Ident//
  | //Call// | -> | ``do`` //ActionName// //Args//
  |  |  **|**  | ``call`` //SubroutineName// //Args//
  | //Args// | -> | //[Exp12]//
  | //[Exp12]// | -> | **eps**
  |  |  **|**  | //Exp12// //[Exp12]//
  | //Dict// | -> | ``{{`` //[DictAssig]// ``}}``
  | //DictAssig// | -> | //Ident// ``=`` //Exp//
  | //[DictAssig]// | -> | **eps**
  |  |  **|**  | //DictAssig//
  |  |  **|**  | //DictAssig// ``,`` //[DictAssig]//
  | //Exp// | -> | ``if`` //Exp// ``then`` //Exp// ``else`` //Exp//
  |  |  **|**  | //Exp1//
  | //Exp1// | -> | //Exp1// ``|`` //Exp2//
  |  |  **|**  | //Exp2//
  | //Exp2// | -> | //Exp2// ``&`` //Exp3//
  |  |  **|**  | //Exp2// ``^`` //Exp3//
  |  |  **|**  | //Exp3//
  | //Exp3// | -> | //Exp4// ``==`` //Exp4//
  |  |  **|**  | //Exp4// ``!=`` //Exp4//
  |  |  **|**  | //Exp4// ``<`` //Exp4//
  |  |  **|**  | //Exp4// ``>`` //Exp4//
  |  |  **|**  | //Exp4// ``<=`` //Exp4//
  |  |  **|**  | //Exp4// ``>=`` //Exp4//
  |  |  **|**  | //Exp4//
  | //Exp4// | -> | //Exp4// ``+`` //Exp5//
  |  |  **|**  | //Exp4// ``-`` //Exp5//
  |  |  **|**  | //Exp5//
  | //Exp5// | -> | //Exp5// ``*`` //Exp6//
  |  |  **|**  | //Exp5// ``/`` //Exp6//
  |  |  **|**  | //Exp5// ``%`` //Exp6//
  |  |  **|**  | //Exp6//
  | //Exp6// | -> | //Exp6// ``<<`` //Exp7//
  |  |  **|**  | //Exp6// ``>>`` //Exp7//
  |  |  **|**  | //Exp6// ``|>>`` //Exp7//
  |  |  **|**  | //Exp7//
  | //Exp7// | -> | ``~`` //Exp7//
  |  |  **|**  | //Exp8//
  | //Exp8// | -> | //Call//
  |  |  **|**  | //Exp9//
  | //Exp9// | -> | //Exp9// //Exp10//
  |  |  **|**  | //Exp10//
  | //Exp10// | -> | //Exp10// ``::`` //Type//
  |  |  **|**  | //Exp11//
  | //Exp11// | -> | ``fl`` //String//
  |  |  **|**  | //Exp12//
  | //Exp12// | -> | //VarName//
  |  |  **|**  | //Integer//
  |  |  **|**  | //Exp12// ``-->`` //FieldName//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Exp// ``,`` //[Exp]// ``)``
  |  |  **|**  | //Exp12// ``.`` //Integer//
  |  |  **|**  | ``_``
  |  |  **|**  | //String//
  |  |  **|**  | //Dict//
  |  |  **|**  | //Exp13//
  | //Exp13// | -> | ``(`` //Exp// ``)``
  | //[Exp]// | -> | //Exp//
  |  |  **|**  | //Exp// ``,`` //[Exp]//



%% File generated by the BNF Converter (bnfc 2.9.4.1).
