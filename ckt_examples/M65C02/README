A simple processor with an example of a (very simple) verification of
one of its operations.

The main file is M65C02.fl.


Due to changes in the clocking library, the verification does no longer
go through. Some timing assumptions are incorrect.  Any volunteer to fix them?
