load "ste.fl";
load "examples/isomatch.fl";

//------------------------------------------------------------------------------
// Load example files.

let p = verilog2pexlif "-Iexamples/verilog"
                       "mux4"
                       ["small.v","small_lib.v"]
                       [];

let s = pexlif2fsm p;

//------------------------------------------------------------------------------
// Testing.

let zeros = flip replicate 0;

//------------------------------------------------------------------------------
all NOT [T,T] = T;
any NOT [F,T] = T;
equal T T;
const T F = T;
flip const T F = F;
curry const (T,F) = T;
uncurry id T F = (T,F);
first NOT (T,T) = (F,T);
second NOT (T,T) = (T,F);
butfirst [F,T] = [T];
//------------------------------------------------------------------------------
// Fin.